소스코드 저장된 폴더에서 git bash 실행
$git init
$git remote add origin "주소"
$git add main.cpp 또는 $git add *
$git status
$git commit -m "실습 1"
$git push origin master

다른 폴더에서
$git clone "주소"
$git add ~
$git status
$git commit -m "실습 2"
$git push origin master
